import Viewport from '~/interfaces/Viewport';
declare const _default: (zoom: string, pageView: HTMLDivElement, defaultViewport: Viewport) => number;
/**
  * Calculate the page zoom ratio.
  *
  * @param   {string} zoom
  * @returns {number}
  */
export default _default;
