import Point from '~/interfaces/Point';
import Rect from '~/interfaces/Rect';
declare const _default: (point: Point, rect: Rect) => boolean;
/**
  * Check if there is a point inside the rectangle.
  *
  * @param    {Point} point
  * @param    {Rect} rect
  * @returns  {boolean}
  */
export default _default;
